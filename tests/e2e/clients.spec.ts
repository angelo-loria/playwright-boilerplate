import { test, expect } from "../../pageObjects/pageFixture";

// test.describe("Clients page", () => {    
//     test("should navigate to Clients", async ({ homePage }) => {
//         expect(await homePage.sideBar.host.isVisible()).toBe(true);
//     });
// });
